spring:
  application:
    name: ms-hw-6-auth
  main:
    cloud-platform: kubernetes
  cloud:
    kubernetes:
      config:
        enabled: true
      reload:
        enabled: true
        monitoring-config-maps: true
        strategy: refresh
        mode: event